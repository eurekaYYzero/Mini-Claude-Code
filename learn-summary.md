# 学习记录
## 第一章
### Loop
完整的多轮：发送消息给模型->模型回复->调用工具->拿到执行结果(Tool_Result)->回写工具结果到state(messages)

## 第二章
### 工具
生产级工具：
- 每个 `tool_use` 块必须有对应的 `tool_result`
  - 通过 `tool_use_id` 进行关联
- `user` / `assistant` 消息必须严格交替
  - 不能连续出现两条相同角色的消息
- 只能使用协议中定义的字段
  - 传入内部元数据会导致 `400` 错误

以上三点是最基础的要求，随着业务复杂还需要引入
权限、上下文、MCP Client、文件系统、query跟踪等机制